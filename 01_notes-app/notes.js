'use-strict';
function getNotes(note) {
    console.log(note);
};

function addNote(note) {
    console.log(note);
};

function removeNote(note) {
    console.log(note);
};

module.exports = {
    getNotes,
    addNote,
    removeNote
};